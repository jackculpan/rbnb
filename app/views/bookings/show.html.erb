<div class="container">
  <% if current_user.business %>
    <%= render "bookings" %>
  <% end %>
  <%= render "reservations" %>
  <%= link_to "View all pools", root_path, class: "btn btn-primary" %>
</div>
